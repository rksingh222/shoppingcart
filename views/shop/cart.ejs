<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
            <main>
                <% if (products.length> 0) { %>
                    <ul>
                        <% products.forEach(p=> { %>
                            <li>
                                <p>
                                    <%= p.productData.title %>(<%= p.qty %>)
                                </p>
                                <div class="buttonSeperator">
                                    <!--<form action="/addqty-cart-item" method="POST">
                                        <input type="hidden" name="productId" value="<%= product.id %>">
                                        <button class="btn"><i class="fa-solid fa-plus"></i></button>
                                    </form>
                                    <form action="/deleteqty-cart-item" method="POST">
                                        <input type="hidden" name="productId" value="<%= product.id %>">
                                        <button class="btn"><i class="fa-solid fa-minus"></i></button>
                                    </form>-->
                                    <!--<a href="/cart/<%= product.id %>" class="btn"><i class="fa-solid fa-plus"></i></a>-->
                                    <form action="/cart/add" method="POST" onsubmit="return disableButton();">
                                        <input type="hidden" name="productId" value="<%= product.id %>">
                                        <button class="btn" id="register"><i class="fa-solid fa-plus"></i></button> 
                                    </form>
                                    <form action="/delete-cart-item" method="POST">
                                        <input type="hidden" name="productId" value="<%= product.id %>">
                                        <button class="btn">Delete</button>
                                    </form>
                                </div>
                            </li>
                            <% }) %>
                    </ul>
                    <% } else { %>
                        <h1>No Products in Cart!</h1>
                        <% } %>
            </main>


            <%- include('../includes/end.ejs') %>